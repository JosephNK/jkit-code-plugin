interface:
  display_name: "Commit"
  short_description: "변경 사항을 분석해 한국어 커밋 메시지 3개를 제안"
  brand_color: "#F05133"
  default_prompt: "Use $commit to stage current changes, analyze the diff, and suggest three Conventional Commits messages (Korean by default) that satisfy commitlint subject-case rules."
policy:
  allow_implicit_invocation: true
