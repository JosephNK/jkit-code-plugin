## EasyLocalization

- Use `easy_localization` — access via `'key'.tr()` or `'key'.tr(args: [value])`
- Key naming: `snake_case` grouped by feature (e.g., `product_title`, `login_email_hint`)
- Translation files: `assets/translations/`
