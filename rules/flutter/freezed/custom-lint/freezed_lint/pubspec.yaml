name: freezed_lint
version: 0.3.5
description: Flutter freezed convention linter (analysis_server_plugin).

publish_to: "none"

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  analyzer: ^10.0.0
  analysis_server_plugin: ^0.3.8
