name: architecture_lint
version: 0.2.33
description: Flutter hexagonal architecture linter (custom_lint plugin).

publish_to: "none"

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  analyzer: ^8.0.0
  custom_lint_builder: ^0.8.1
  glob: ^2.1.0
  path: ^1.0.0
